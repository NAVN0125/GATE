def checknum(userstrinput):
    for x in userstrinput:
        for y in range (numset.len()):
			if numset[y] = userstrinput[x]:
		    	nonumber += 1

def checkcase(userstrinput):
    for x in userstrinput:
        for y in range (lowercase.len()):
			if lowercase[y] = userstrinput[x]:
		    	nolowercase += 1
			elif uppercase[y] = userstrinput[x]:
		    	nouppercase += 1